[package]
name = "tela-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
tela-engine = { path = "../tela-engine" }
clap = { version = "4", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
